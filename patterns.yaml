secrets:
  AWS Access Key: "AKIA[0-9A-Z]{16}"
  API Key: "(?i)\\bapi[-_]?key\\b\\s*[:=]\\s*[\"']?[A-Za-z0-9-_]{20,}[\"']?"
  Hardcoded Password: "(?i)\\b(password|passwd|pwd|pass)\\b\\s*[:=]\\s*[\"']?[^\"'\\n]{6,}[\"']?"
  JWT Token: "eyJ[a-zA-Z0-9]{30,}\\.[a-zA-Z0-9_-]{30,}\\.[a-zA-Z0-9_-]{10,}"
  Bearer Token: "(?i)\\bBearer\\s+[a-zA-Z0-9-_]+"
  SSH Private Key: "(?s)-----BEGIN (RSA|OPENSSH|EC|DSA) PRIVATE KEY-----(.*?)-----END (RSA|OPENSSH|EC|DSA) PRIVATE KEY-----"
  Google API Key: "AIza[0-9A-Za-z-_]{35}"
  Secret Key: "(?i)\\bsecret[-_]?key\\b\\s*[:=]\\s*[\"']?[^\"'\\n]{6,}[\"']?"
# Если не нужны предупреждения о потенциально опасных вызовах, удалите или закомментируйте секцию unsafe
#unsafe:
#  JavaScript Eval: "(?s)\\beval\\s*\\(\\s*[^)\\s].+?\\)"
#  JavaScript New Function: "(?s)\\bnew\\s+Function\\s*\\(\\s*[^)\\s].+?\\)"
